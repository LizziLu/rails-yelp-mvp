<%@restaurants.each do |r| %>
<h2><%= link_to r.name, restaurant_path(r) %></h2>
<h5>Adress:<%=  r.address %> Phone Number: <%= r.phone_number%></h5>
<%end%>

<%= link_to "+Add new restaurant", new_restaurant_path%>
